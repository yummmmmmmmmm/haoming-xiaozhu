# 好命小猪 · 开发计划与执行记录

> 本文件是本次开发的计划、执行结果与验收对照表。验收标准全部来自 `PRD.md` 第 9 节。
> 最后更新：全部阶段已完成，`89` 项自动化用例全绿。

---

## 一、目标与原则

1. 以 `PRD.md` 为唯一规格来源（PRD 开篇已声明「以本文为准」）。
2. 按阶段顺序推进，每个阶段都**先补有效测试、跑通再进入下一阶段**。
3. 测试失败先定位根因：是**应用缺陷**就改应用；是**测试脚手架**问题就修脚手架，绝不放宽验收标准。

---

## 二、阶段计划与完成情况

| 阶段 | 计划内容 | 状态 | 产出 / 证据 |
| --- | --- | --- | --- |
| 0 | 环境与基线：测试脚手架、依赖、typecheck、build、dev 服务 | ✅ 完成 | `scripts/test.mjs`、`tests/harness/*`；17 项基线用例；四类工程命令全通过 |
| 1 | 文档一致性：修正 PRD 与实现不符之处 + 文档一致性测试 | ✅ 完成 | PRD 两处描述修正、补第 13 件商品；`tests/prd.doc.test.ts` 6 项 |
| 2 | 验收自动化：批次 A→H 逐个补齐 | ✅ 完成 | 8 个验收批次文件，共 62 项 |
| 3 | 缺陷修复与回归 | ✅ 完成 | 修复 3 个真实缺陷（见第四节），全量回归通过 |
| 4 | 交付收尾：typecheck + build + 全量测试 | ✅ 完成 | typecheck 0 错误；build 成功；12 个文件 89 项全绿 |
| 5 | 计划与验收记录落盘 | ✅ 完成 | 本文件 |

### 各测试文件与用例数

| 文件 | 用例数 | 覆盖 |
| --- | --- | --- |
| `tests/baseline.content.test.ts` | 7 | 内容基线（分类/文章/商品数量、字段完整性） |
| `tests/repo.data.test.ts` | 10 | 数据层：级联删除、多用户隔离、排序、重置 |
| `tests/prd.doc.test.ts` | 6 | PRD 文档与实现一致性 |
| `tests/a.auth.guard.test.tsx` | 11 | 登录/注册/记住密码/路由守卫/授权占位 |
| `tests/a.routes.smoke.test.tsx` | 6 | 19 个路由冒烟 + 无档案空态 |
| `tests/b.home.test.tsx` | 9 | 首页轮播、tab 切换与滑动、分类、智能问疾 |
| `tests/c.excerpt.test.tsx` | 5 | 摘录进饲养日记（C1–C7） |
| `tests/d.account.test.tsx` | 8 | 账户页（D1–D7） |
| `tests/e.naming.test.tsx` | 4 | 命名一致性（E1–E4） |
| `tests/f.record.test.tsx` | 9 | 记录模块（F1–F9） |
| `tests/g.forum.test.tsx` | 8 | 论坛（G1–G8） |
| `tests/h.shop.test.tsx` | 6 | 商城（H1–H7） |
| **合计** | **89** | **0 失败** |

---

## 三、测试基础设施

项目路径包含 `#`，Vite/Vitest 无法解析该路径，因此改为**基于项目已有 esbuild 工具链**自建测试运行器：

- 运行器：`scripts/test.mjs`（esbuild 打包 + `node:test`）
- 环境引导：`tests/harness/register.mjs`（jsdom + fake-indexeddb + Testing Library）
- 组件脚手架：`tests/harness/app.tsx`（预置数据、自动登录、局部 DOM 查询）

命令：

```bash
npm test                  # 运行全部测试
node scripts/test.mjs a.auth   # 只运行文件名包含 a.auth 的测试
```

引导模块中三处**必要的环境适配**（均为测试环境限制，与应用代码无关）：

1. `@testing-library/react` 必须等 jsdom 就绪后动态导入，否则其内部 `screen` 初始化失败。
2. jsdom 不加载图片，需补 `Image` 的最小实现，否则上传照片流程会永久等待。
3. Node 自带全局 `File`/`Blob` 会与 jsdom 的 `FileReader` 不兼容，统一换成 jsdom 实现。

> 另外，`@testing-library` 的 `waitFor` 在本运行器下会耗尽内存，脚手架改为自建 `until()` 轮询等待。
>
> 脚手架还固定了 `seedPet` 的 `createdAt` **严格递增**：`listPets` 按创建时间升序决定「默认猪猪」，若两只猪落在同一毫秒，排序会退化为主键（id）顺序，导致「多猪数据隔离」类用例（F8）偶发失败。真实 UI 交互不可能同毫秒建两只猪，故按「脚手架问题修脚手架」处理，不放宽任何验收标准。

---

## 四、缺陷修复记录

| 编号 | 现象 | 根因 | 修复 |
| --- | --- | --- | --- |
| D-1 | 登录页点「微信登录 / 抖音登录」看不到任何提示，与 PRD 2.4「点击提示待接入」不符 | 轻提示容器 `<Toasts />` 只挂在受保护布局里，登录页路由不含它，`toast()` 调用后无处渲染 | 把 `<Toasts />` 提升到 `App` 顶层（`Routes` 之外），移除布局内的重复挂载 |
| D-2 | 首页轮播第 1 张（论坛帖）点击后跳到**论坛列表页**，而非帖子详情页（违反验收 B1） | `onSlideSelect` 的论坛分支依赖**异步加载的 `posts` 状态**；轮播在数据加载完成前即可点击，此时 `posts` 为空数组，于是退化为 `navigate('/forum')` | 改为直接按 slide 的 `targetId` 跳转，不再依赖任何异步状态 |
| D-3 | PRD 5.2 声明商品 13 件，实际仅有 12 件 | 实现遗漏 | 按规格补齐第 13 件商品 |
| D-4 | PRD 两处描述与实现不一致：模块入口写了「首页记录弹层」，轮播文案写成「健康 / 新品零食」 | 文档落后于实现 | 修正 PRD 描述，并加入文档一致性测试防回归 |

> D-1、D-2 是自动化测试跑出来的真实用户可见缺陷；D-3、D-4 属文档一致性问题。

---

## 五、验收对照表（PRD 第 9 节）

### A. 登录与路由守卫 — `tests/a.auth.guard.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| A1 | 未登录访问受保护页面跳转登录页（6 条路径逐一验证） | ✅ |
| A2 | 用户名 1 个字符 → 提示「用户名至少 2 个字符」 | ✅ |
| A3 | 密码 3 位 → 提示「密码至少 4 位」 | ✅ |
| A4 | 重复用户名注册 → 提示「这个用户名已经被注册了」 | ✅ |
| A5 | 正确登录 → 进入首页，底部显示「你好，昵称 🐹」 | ✅ |
| A6 | 勾选记住账号密码后刷新 → 账号密码回填且勾选框选中 | ✅ |
| A7 | 刷新页面 → 需要重新登录（登录态只存内存） | ✅ |
| 附加 | 未勾选时不写入本机；用户名不存在 / 密码错误均有明确提示；微信/抖音授权占位提示 | ✅ |

### B. 首页 — `tests/b.home.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| B1 | 点轮播第 1 张 → 跳到对应详情页（帖子/文章/商品，4 张逐一验证） | ✅（曾失败，修复 D-2） |
| B2 | 点「论坛分享」→ 内容区切换为帖子列表 | ✅ |
| B3 | 内容区向左/右滑动切换 tab，且边界不越界 | ✅ |
| B4 | 指南 tab 显示 6 个分类按钮 | ✅ |
| B5 | 智能问疾位于六个分类按钮正下方 | ✅ |
| B6 | 点智能问疾 → 弹窗含免责声明且声明不做确诊/开药 | ✅（曾误判，已按原文修正断言） |
| B7 | 点分类「饮食营养」→ 只列出该分类文章 | ✅ |
| B8 | 点「新手必看」第一篇 → 详情正文小标题逐个正常渲染 | ✅ |

### C. 摘录进饲养日记（本版重点） — `tests/c.excerpt.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| C1 | 指南文章摘录 → 预填标题、摘要、首段与要点 | ✅ |
| C2 | 改几句再保存 → 提示「已记入饲养日记 📔」，弹窗关闭 | ✅ |
| C3 | 日记列表最前一条为该摘录，显示「摘录自 饲养指南 · 标题」 | ✅ |
| C4 | 帖子摘录 → 预填话题、作者、正文、**最多 3 条**回复（第 4 条不摘） | ✅ |
| C5 | 帖子摘录保存后列表显示「摘录自 论坛摘录 · 话题」 | ✅ |
| C6 | 无档案时摘录 → 提示先建档，且不提供保存入口（不产生脏数据） | ✅ |
| C7 | 有 2 只猪时出现「记给谁」，可指定记到某一只名下 | ✅ |

### D. 账户页 — `tests/d.account.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| D1 | 区块顺序：资料卡 → 四统计 → 我的荷兰猪 → 个人相册 → 我的购买 → 设置 → 退出登录 | ✅ |
| D2 | 不存在「我的发帖」区块或入口 | ✅ |
| D3 | 四统计依次为 荷兰猪 / 饲养日记 / 相册照片 / 健康打卡 | ✅ |
| D4 | 无照片时显示空态「相册还是空的…」 | ✅ |
| D5 | 上传 1 张相册照片 → 数字 +1 且出现在九宫格 | ✅ |
| D6 | 发一条带图帖子 → 配图也出现在个人相册 | ✅ |
| D7 | 重置本机数据 → 提示已重置并回到登录页；重新注册后档案/记录/照片/订单均为 0 | ✅ |

### E. 命名一致性 — `tests/e.naming.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| E1 | 快速记录弹层第 5 项为「饲养日记」 | ✅ |
| E2 | 该记录页标题为「饲养日记」 | ✅ |
| E3 | 猪猪档案页入口显示「饲养日记」，副文案「记录了多少篇小日常」 | ✅ |
| E4 | 全局搜索「猪猪日记」不再出现（源码 41 个文件 + 4 个页面） | ✅ |

### F. 记录模块 — `tests/f.record.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| F1 | 无档案时 6 个记录页显示建档引导空态，不报错 | ✅（含在 `a.routes.smoke`） |
| F2 | 新增体重 720g → 列表出现、最新 720g、曲线 1 个数据点 | ✅ |
| F3 | 先轻后重显示增长；变轻时显示负数并标红 | ✅ |
| F4 | 新增后删除 → 列表移除、统计同步、曲线点消失 | ✅ |
| F5 | 健康打卡新增「疫苗」→ 疫苗计数 +1 | ✅ |
| F6 | 待办「换垫料」周期 7 天，完成后自动顺延到 7 天后 | ✅ |
| F7 | 相册上传 2 张 → 提示 + 九宫格两张 + 看大图 + 删除 | ✅ |
| F8 | 2 只猪切换 → 体重与日记各页数据互不串 | ✅ |
| F9 | 删除猪档案 → 体重/健康/日记/相册/待办一并删除，其他猪不受影响 | ✅ |

### G. 论坛 — `tests/g.forum.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| G1 | 默认「全部」，可见内置示例帖 8 条 | ✅ |
| G2 | 点话题「新手求助」只显示该话题帖子 | ✅ |
| G3 | 点爱心 🤍→❤️ 且 +1，再次点击取消 | ✅ |
| G4 | 正文与图片都留空发布 → 被拦截并提示，不产生帖子 | ✅ |
| G5 | 写正文 + 2 张图发布 → 列表最前，作者为当前昵称，带 2 张配图 | ✅ |
| G6 | 回复帖子 + 回复某条回复 → 显示「回复 @某人：」 | ✅ |
| G7 | 删除自己的帖子 → 二次确认（可取消）后帖子与全部回复一起消失 | ✅ |
| G8 | 别人的帖子不显示删除按钮 | ✅ |

### H. 商城 — `tests/h.shop.test.tsx`

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| H1 | 切换分类「笼具用品」只显示该分类商品 | ✅ |
| H2 | 加入购物车 → 提示 + 底部导航商城角标 +1 | ✅ |
| H3 | 数量减到 0 → 该条目从购物车移除 | ✅ |
| H4 | 点「结算」→ 确认下单弹窗，可填收货地址 | ✅ |
| H5 | 确认下单 → 生成订单并跳转订单页，购物车清空 | ✅ |
| H6 | 订单页显示订单号后 8 位、商品明细、单价×数量、合计、状态、地址 | ✅ |
| H7 | 地址留空 → 地址显示「本机演示订单（未填写收货地址）」 | ✅ |

### I. 工程与调试

| 编号 | 验收要点 | 结果 |
| --- | --- | --- |
| I1 | `npm run typecheck` 无类型错误 | ✅ 0 错误（含 tests 目录） |
| I2 | `npm run build` 构建成功并产出 `dist/` | ✅ |
| I3 | `npm run dev` 后 `http://127.0.0.1:5173/` 首页返回 200 | ✅ `/`、`/app.js`、`/app.css` 均 200 |
| I4 | IDE 断点调试 | ⏳ 需在 IDE 内手动验证（仓库已备 `.vscode/launch.json`） |
| I5 | 修改页面文案保存后开发服务自动重建，刷新可见 | ✅ 已用 ASCII 标记实测：改文案 → 服务自动重建 → 产物中出现新内容 → 还原 |

---

## 六、手动验收建议路径

自动化用例已覆盖上述全部功能点，仍建议按下列顺序手动点一遍（覆盖端到端手感与样式）：

1. 首次打开 → 应停在登录页 → 注册一个账号 → 进入首页。
2. 首页：滑轮播、点第 1 张轮播（应进帖子详情）、左右滑动切 tab、点分类、点智能问疾。
3. 登录页点微信/抖音登录 → 应看到「待接入」轻提示（D-1 修复点）。
4. 「我的荷兰猪」建一只猪 → 再从首页「＋」进入快速记录，走一遍体重/健康/日记/待办/相册。
5. 指南文章与论坛帖子底部点「📔 记入饲养日记」→ 改几句保存 → 到饲养日记页看来源标记。
6. 账户页：看四统计、个人相册九宫格、我的购买；最后试一次「重置本机数据」（会清空本机数据，请放在最后）。
7. 论坛：发一条带图帖 → 点赞 → 回复并回复某人 → 删除自己的帖子（应二次确认）。

---

## 七、备注

- 测试只覆盖 `src` 与文档约定，不修改任何验收标准；`tests/` 目录不参与构建产物。
- 所有临时调试文件已清理（`tests/tmp.debug.test.tsx` 等）。
